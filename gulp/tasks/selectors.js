/* Minify les nom de class. Non utilisé, à cause de l"utilisation des class dans le code JS de foundation
   Tout est commenté pour éviter d"avoir à installer le plugin */

/*
var gulp	= require("gulp");
var gs		= require("gulp-selectors");
 
 
gulp.task("selectors", function() {
  return gulp.src(config.selectors.src)
    .pipe(gs.run())
    .pipe(gulp.dest(config.selectors.dest));
});
*/